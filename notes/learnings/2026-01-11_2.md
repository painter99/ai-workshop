# Sekce 6: File Handling - Kompletní Funkční Snippety (2026-01-11)
# Shromážděné z role-play, bug-fixů a cvičení. Každý snippet je standalone funkční.
# Použij `with open()` pro best practices, `'w'` přepíše, `'a'` přidá, `\n` explicitně.

## 1. Testování Zápisu (Debugging - Ověření cwd a práv)
```python
# Ověření zápisu do souboru (použito pro diagnostiku prázdného todos.txt)
with open("todos.txt", "w") as f:
    f.write("Test zápisu\n")
print("Soubor vytvořen - zkontroluj cat todos.txt")
```

## 2. Čtení Souboru a Výpis (Cvičení 1: bear.txt)
```python
# Načte a vypíše celý obsah souboru
with open("bear.txt", "r") as file:
    content = file.read()
    print(content)
```

## 3. Manuální Open/Close (Cvičení 1 - Alternativa)
```python
# Stejné jako výše, ale manuální close() (méně bezpečné)
file = open("bear.txt", "r")
content = file.read()
file.close()
print(content)
```

## 4. Modifikace Obsahu (Cvičení 2: essay.txt - .title())
```python
# Načte, převede první písmena slov na uppercase, vypíše
with open("essay.txt", "r") as file:
    content = file.read().title()
    print(content)
```

## 5. Počet Znaků (Cvičení 3: essay.txt)
```python
# Načte a spočítá počet znaků
with open("essay.txt", "r") as file:
    content = len(file.read())
    print(content)  # Očekáváno: 144
```

## 6. Počet Znaků s F-String (Cvičení 4: essay.txt)
```python
# Načte, spočítá a vypíše formátovanou zprávu
with open("essay.txt", "r") as file:
    content = len(file.read())
    print(f"The file contains {content} characters.")  # Tečka na konci!
```

## 7. Vytvoření Jednoho Souboru (Cvičení 5: file.txt)
```python
# Vytvoří nový soubor a zapíše text
with open("file.txt", "w") as f:
    f.write("snail")
```

## 8. ZIP - Více Souborů z Dvoch Listů (Cvičení 6)
```python
countries = ["Albania", "Belgium", "Canada"]
filenames = ["a.txt", "b.txt", "c.txt"]
for country, filename in zip(countries, filenames):
    with open(filename, "w") as f:
        f.write(country)
```

## 9. Append Člena (Cvičení: members.txt)
```python
# Přidá nového člena na konec souboru
name = input("What is your name? ")
with open("members.txt", "a") as file:
    file.write(name + "\n")
```

## 10. Lektorův Append (Alternativa: Read-Append-Write)
```python
# Načte, přidá, zapíše zpět (flexibilnější pro validaci)
member = input("Add a new member: ")
file = open("members.txt", "r")
existing_members = file.readlines()
file.close()
existing_members.append(member + "\n")
file = open("members.txt", "w")
file.writelines(existing_members)
file.close()
```

## 11. Generování Více Souborů (Cvičení: Hello do doc.txt atd.)
```python
filenames = ["doc.txt", "report.txt", "presentation.txt"]
for filename in filenames:
    with open(filename, "w") as file:
        file.write("Hello\n")
```

## 12. Čtení Více Souborů (Cvičení: a.txt, b.txt, c.txt)
```python
filenames = ["a.txt", "b.txt", "c.txt"]
for filename in filenames:
    with open(filename, "r") as file:
        content = file.read()
        print(content)
```

## 13. Bug-Fix 1: Chybějící \n (data.txt - Dva Řádky)
```python
# Oprava: Přidat \n na konec každého write()
file = open("data.txt", "w")
file.write("100.12\n")
file.write("111.23\n")
file.close()
```

## 14. Bug-Fix 2: Špatný Mód (data.txt - 'r' → 'w')
```python
# Oprava: Změnit 'r' na 'w' pro zápis
file = open("data.txt", "w")
file.write("100.12")
file.close()
```

## 15. Role-Play: Uložení Položky (data.txt - Append)
```python
# Uložení itemu na konec souboru
item = input("Enter item: ")
with open("data.txt", "a") as file:
    file.write(item + "\n")
```

## 16. Role-Play: Načtení Položek (data.txt - readlines + strip)
```python
# Načte všechny položky jako list, odstraní \n
with open("data.txt", "r") as file:
    items = file.readlines()
    items = [item.strip() for item in items]
for item in items:
    print(item)
```

## 17. Role-Play: Filtrování Keyword (data.txt - 'Python')
```python
# Načte, filtruje položky obsahující 'Python'
keyword = "Python"
with open("data.txt", "r") as file:
    items = file.readlines()
    items = [item.strip() for item in items]
for item in items:
    if keyword in item:
        print(item)
```

## 18. Více Souborů z Jednoho Listu (Cvičení 7: countries → *.txt)
```python
# Generuje soubory Albania.txt obsahující "Albania"
countries = ["Albania", "Belgium", "Canada"]
for country in countries:
    filename = f"{country}.txt"
    with open(filename, "w") as f:
        f.write(country)
```

# Poznámky k Opakování:
# - Vždy simuluj data flow: Načti → Process → Persistuj.
# - `'w'` = přepíše, `'a'` = přidá, `\n` = manuálně.
# - `with open()` = best practice (automatický close).
# - Debugging: `print(repr(content))` pro skryté znaky, `os.getcwd()` pro cwd.
# Celkem: 18 snippetů → Sekce 6 byla náročná, ale zvládnuta! 🚀
