## Session: List Comprehensions Mastery (2026-01-13)

### Technické koncepty a implementace

**1. List Comprehension Syntax**
- **Základní forma:** `[výraz for proměnná in iterable]` – vytvoří nový list aplikací výrazu na každý prvek iterable.
- **S filtrem:** `[výraz for proměnná in iterable if podmínka]` – vybere jen prvky splňující podmínku.
- **Výhody:** Stručnost (concise), čitelnost (readable), vyšší performance než `for` + `append()`.

**2. Transformace Dat (Data Transformation)**
- **Type Conversion:** `[float(item) for item in strs]` – převede `str` → `float` (např. `['10', '19.1']` → `[10.0, 19.1]`).
- **Matematické operace:** `[n * 2 for n in numbers]` nebo `[x ** 2 for x in range(1, 6)]` – multiplikace, mocniny.
- **String Methods:** `[name.title() for name in names]` – aplikace `.title()`, `.upper()`, `.len()`.

**3. Funkce a Iterables**
- **`range(start, stop)`:** Generátor sekvence (exclusive stop): `range(1, 6)` → `1,2,3,4,5`.
- **`sum(list)`:** Součet iterovatelného (funguje na `list[int/float]`).
- **Nested Comprehensions:** Později – `[item for sublist in nested_list for item in sublist]`.

**4. Debugging & Common Patterns**
- **Scope Check:** Proměnná v `for` musí existovat (NameError při typo: `user_entires` místo `user_entries`).
- **Function Call:** `range(1, 6)` zavolat přímo v comprehension: `[x**2 for x in range(1, 6)]`.
- **Output:** `sum()` vrací hodnotu – vždy `print(sum(...))` nebo `total = sum(...)`.

**5. Role-Play & Application**
- **Squares (1-5):** `[x**2 for x in range(1, 6)]` → `[1, 4, 9, 16, 25]`.
- **Cubes (10-15):** `[x**3 for x in range(10, 16)]` → `[1000, 1331, 1728, 2197, 2744, 3375]`.

### Vyřešené problémy (Bug-Fixing)
- **NameError:** Typo v proměnné `user_entires` → `user_entries`. ✅
- **Range Misuse:** `for i in range` (funkce bez volání) → `for i in range(1, 6)`. ✅
- **Sum Visibility:** `sum(list)` bez `print()` → `total = sum(...)`; `print(total)`. ✅
- **Quiz Syntax:** `[for i in old i + 10]` → `[i + 10 for i in old]`. ✅

### Blockers & Resolution
- **Variable Shadowing:** Pokus `range = [1, 6]` → Zabráněno: Nepřepisovat built-ins. ✅
- **Range Bounds:** `range(1, 5)` → `range(1, 6)` (inclusive start, exclusive stop). ✅
- **Role-Play Pressure:** "Nevím" → Anna ukázala `[x**2 for x in range(1, 6)]`; aplikováno na cubes. ✅

### Next Steps
- Dokončit Sekci 7: Filtrování (`if`), nested comprehensions, dict comprehensions.
- Přechod na **Sekci 8: Master File Management (Context Managers, With Statement)**.
- Procvič: `[n for n in numbers if n > 15]`, `sum([float(s) for s in strs])`.

### Engineering Summary
"List comprehension = 'for loop na steroidech'. Nahrazuje boilerplate kód jedním řádkem, zachovává čitelnost a zvyšuje efektivitu. Klíč: Syntax `[output for item in iterable]`, scope check, function calls v místě. Rozumět 'proč' (concise/readable) > memorování syntaxe."

---

**Absolvovaná lekce:** Sekce 7/57 (List Comprehensions) ✅
**Role Play Goals:** 3/3 completed (squares + cubes challenge) 🎯
**Key Takeaway:** `[operace for item in iterable]` – vždy simuluj data flow (input → transform → output). Typo/scope = mechanical, range volání = process. 💡
