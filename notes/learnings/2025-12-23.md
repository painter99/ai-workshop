# Session: FOR-Loops, Pattern Matching & Logic-First Thinking (2025-12-23)

### Co jsem se naučil

**1. Match-Case + Bitwise OR Operator (`|`)**
- Operátor `|` v `match-case` = "NEBO" (pattern matching, ne logické `or`)
- Zkrácená notace: `case "USA" | "United States":` místo dvou case bloků
- `|` v match-case ≠ `or` v podmínkách (syntaktický rozdíl, jiný kontext)

**2. FOR-Loop = Určitý cyklus (Definite Loop)**
- FOR iteruje přes ZNÁMÝ počet prvků (seznam, range, string)
- WHILE iteruje podle podmínky (neurčitý cyklus)
- Iterační proměnná (např. `member`, `day`) se mění v každém kroku – FOR ji automaticky vytváří
- Tělo smyčky MUSÍ být odsazené (IndentationError, pokud není)
- Pořadí je kritické: seznam musí být definován PŘED FOR-loopem (NameError)

**3. String Metody v FOR-Loop**
- `.title()` = každé slovo s velkým prvním písmenem
- `.capitalize()` = jen první písmeno (nebo první písmeno prvního slova)
- `.upper()` / `.lower()` = všechna velká/malá
- **Důležité:** metoda se volá s `()` – bez nich se jen vyvolá objekt metody, nespustí se

**4. Python vs. CPython vs. Interpreter (Fundamenty)**
- **Python** = abstraktní programovací jazyk (specifikace)
- **CPython** = konkrétní implementace Pythonu (napsaná v C, nejčastější)
- **Interpreter** = program, který přeloží tvůj kód → bytecode → procesor
- Tok: Tvůj kód → Lexer (znaky) → Parser (syntaxe) → Compiler (bytecode) → VM (spuštění)
- CPython je hybrid: nejdřív kompiluje na bytecode, pak interpretuje

**5. Logika PŘED Syntaxí (Klíčový mindset)**
- **NEJDŘÍV:** "Co se má stát? Jaká je logika?" (simulace v hlavě)
- **PAK:** "Jak se to píše?" (syntaxe)
- Když se zaměříš jen na syntaxi, ztrácíš se v detailech
- Když začneš logikou, syntaxe je triviální

**6. Vzory Bug-Fixingu**
- **NameError** = "Proměnná neexistuje v tom místě" (pořadí kódu)
- **IndentationError** = "Odsazení chybí" (tělo smyčky/podmínky)
- **SyntaxError** = "Špatná syntaxe" (závorky, středníky)
- **TypeError/Logic** = "Metoda se nevolá s `()`" (např. `capitalize` vs. `capitalize()`)

### Překážky a jejich řešení
- Zpočátku jsem měl pocit, že CPython dělá jakoby "v pozadí" → porozuměl jsem až po vysvětlení toku překladu
- Soustředil jsem se příliš na syntaxi → až když jsem začal simulovat logiku v hlavě, viděl jsem rozdíly
- DataCamp byl příliš pokročilý bez fundamentů → správné rozhodnutí vynechat a pokračovat v TPMC

### Další kroky
- Sekce 4 v Udemy TPMC
- Budoucí témata: `break`/`continue`, `enumerate()`, `zip()`, list comprehension, vnořené smyčky
- IF-ELSE podmínky (složitější logika než match-case)
- Slovníky a metoda `.items()`

### Citát lekce
"Nejdřív logika, pak syntaxe. Syntaxe je jen implementace myšlenky – pokud nemáš správnou myšlenku, syntaxe tě nezachrání."

---

**Absolvovaná lekce:** Sekce 3/57 (Match-Case, FOR-Loop, Bug-Fixing) ✅ 3 ze 4 správně na závěrečním kvízu 🎯
